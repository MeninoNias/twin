# Discord
DISCORD_TOKEN=
DISCORD_USER_ID=

# Database (PostgreSQL with pgvector — matches docker-compose defaults)
DATABASE_URL=postgresql://twin:twin@localhost:5432/twin

# OpenAI (required for embeddings, regardless of LLM_PROVIDER)
OPENAI_API_KEY=

# LLM provider: "openai", "anthropic", or "google"
# OpenAI reuses OPENAI_API_KEY above — no extra key needed
LLM_PROVIDER=openai

# Only needed if using anthropic or google as LLM_PROVIDER
# ANTHROPIC_API_KEY=
# GOOGLE_GENERATIVE_AI_API_KEY=
